{
  "name": "ferrari-f1-website",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cp -r dist/. ..",
    "start": "node backend/server.js"
  }
}
